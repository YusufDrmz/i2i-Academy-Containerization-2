# i2i-Academy-Containerization-2

Deployed a static webpage using Docker and nginx, both locally and on a GCP VM.

## What I did

- Created an index.html with a custom message
- Wrote a docker-compose.yml using the nginx:latest image
- Ran the container locally and verified it at localhost:8080
- Deployed the same setup on a GCP VM and accessed it via public IP
